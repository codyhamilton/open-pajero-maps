# Brief: 1-08 — Schema rows for every WP2-WP5 unknown, each with a first test

Consumer: documentation worker; consumed by WP2/WP3 planning and 1-09.
Owned paths: `docs/schema/route-planning.md`, `docs/schema/index-idx.md`, `docs/schema/flags.md`, `docs/schema/UNKNOWNS.md` (regenerated only), plus any other `docs/schema/*.md` where a row must live. Touch nothing outside `docs/schema/`.
Commits: Commit to the current branch when done evidence passes.
Depends on: nothing.
Runs alongside: 1-01 through 1-07 (disjoint paths).
Budget: 8 files to read, about 150 lines to change, 35 tool turns. Past the budget, stop: write a handoff under this brief's name in `IMPLEMENTATION.md`, commit, and report `over budget`.

## Required reading, in order

1. `docs/plans/03-map-layer-parity-remediation/DESIGN.md` — "Architectural Implications", Stable docs bullet (lists the unknowns); Phase 1 "Also delivers" last sentence.
2. `docs/plans/03-map-layer-parity-remediation/FINDINGS.md` — section 4 (structural source differences for later work packages).
3. `docs/schema/README.md` — row format and status vocabulary (verified / observed / spec-only / assumed / unknown).
4. `docs/schema/UNKNOWNS.md` (grep only; generated by `parser/tools/lint_schema.py --write`).

Read ranges and grep; do not read a whole file to find one section, do not re-read a file already in context, and truncate long tool output.

## Goal

Every WP2-WP5 unknown named in the source analysis has a `docs/schema/` row with status `unknown` and a stated first test, so none is re-derived later.

## Contract

Cited unknowns: ext frames `0xAF100100`/`0xAF100300`, turn-restriction yield (1 of 57,372 restriction relations resolved in region 178), POI vendor category codes, suburb hierarchy, undecoded WP4 families, plus any further WP2-WP5 item in FINDINGS section 4. Rows already present are not duplicated: audit first (`grep` UNKNOWNS.md), add only what is missing, and add a "first test" (a concrete offline check: what to run or compare) to any existing row for these items that lacks one. No new claims: every row cites FINDINGS or the spec; nothing is asserted beyond the source.

## Changes

- Add rows in the schema file's existing format. Run `.venv-rp/bin/python parser/tools/lint_schema.py --write` to regenerate `UNKNOWNS.md`; never hand-edit it.

### Keep untouched

Existing row statuses and text other than adding a first test.

## Done evidence

- `.venv-rp/bin/python parser/tools/lint_schema.py` → exits 0 (lint clean), `UNKNOWNS.md` regenerated.
- A checklist in your report maps each cited unknown to the schema row (file:line) and its first test.

## Report back

Under 1,500 tokens. Status: `done` | `done with concerns` | `blocked` | `needs context` | `over budget`. Then what changed, the check output before and after, any deviation from this brief and why, and any contradiction between this brief and the contracts it cites. Never resolve a contradiction silently.

A non-trivial bug outside your done evidence: report symptom, location, and root cause if found. Do not fix it here.

Do not spawn agents beyond read-only research helpers. If this unit needs one, it was mis-sized: report `blocked` and say so.
